# Importers

This folder documents how **external exports** map into the hub’s canonical markdown.

- **Spec:** `spec.md`
- **Worked flows:** `../docs/import-workflows.md`
- **CLI:** `../bin/memory-bridge import`

The Python importer lives at `scripts/memory_bridge/importer.py`.
